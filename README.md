# sync-files-go

個人専用オンラインファイル同期システム。バックエンドは **Go（標準ライブラリ中心）**、フロントエンドは **HTMX**、ストレージは **AWS S3 Files（NFS マウント）**、メタデータは **RDS for MySQL（Primary + Read Replica、DBRouter で読み書き分離）**、デプロイは **Docker + Terraform + ECR + ECS Fargate**、外部公開は **Cloudflare Tunnel + サイドカー nginx**（ALB は不採用）。

## ステータス

設計フェーズ。実装はこの後。

## 設計の出発点

「意図せぬファイル削除の自動実行を発生させない」ことを最重要の不変条件として、すべての破壊的操作を **明示的同意 × ソフト削除 × バージョン履歴** の三段ガードで包む。同期は **OCC（楽観的同時実行制御） + コンフリクトコピー** を採用し、自動マージは行わない。

## ドキュメント

設計のすべての判断と詳細は [`docs/`](./docs/) を参照。読み始めは [`docs/README.md`](./docs/README.md)。

## ライセンス

未定（個人利用前提）。
